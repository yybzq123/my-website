
server:
  port: 80
  servlet:
    context-path: /
spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    druid:
      url: jdbc:mysql://8.148.4.75:3306/cm_clock
      username: cm_clock
      password: cm_clock
      driver-class-name: com.mysql.cj.jdbc.Driver
      # ?????????????
      initial-size: 5
      # ??????????
      min-idle: 5
      # ?????????
      max-active: 20
      # ????????????????
      max-wait: 60000
      # ??????????????????timeBetweenEvictionRunsMillis???validationQuery?????????
      test-while-idle: true
      # ?????????????testWhileIdel?????
      time-between-eviction-runs-millis: 60000
      # ????????????????????????????????????(??????????????)
      min-evictable-idle-time-millis: 30000
      # ??????????????sql ?????????(oracle?? select 1 from dual)
      validation-query: select 1
      # ????????validationQuery????????,???????,???true
      test-on-borrow: false
      # ????????validationQuery????????,???????,???true
      test-on-return: false
      # ????preparedStatement, ???PSCache,PSCache???????????????????oracle,?mysql??????
      pool-prepared-statements: false
      # ???PSCache???????0????0??poolPreparedStatements???????true??Druid??????Oracle?PSCache??????????????????????????100
      max-pool-prepared-statement-per-connection-size: -1
      # ????DruidDataSource?????
      use-global-data-source-stat: true

mybatis:
  configuration:  # setting??
    auto-mapping-behavior: full
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  type-aliases-package: com.spring.pojo # ????
  mapper-locations: classpath:/mapper/*.xml # mapperxml??